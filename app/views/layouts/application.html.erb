<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <div class="d-flex flex-column flex-md-stretch align-items-center p-3 px-md-4 mb-3 bg-white border- shadow-large">
      <h5 class="my-0 mr-md-auto font-weight-normal">Блог для тестового задания</h5><br>
      <br />
      <nav class="my-2 my-md-0 mr-md-3">
        <span><%= link_to "Список постов", home_path, :class => "btn btn-secondary" %></span>
        <% if user_signed_in? %>
          <span><%= link_to "Новый пост", new_post_path, :class => "btn btn-secondary" %> </span><br>
          <br />
        <% end %>
      </nav>
      <% if user_signed_in? %>
        <span><%= link_to 'Выйти', destroy_user_session_url, :class => "btn btn-danger btn-sm", method: :delete %></span>
        <br>
        <span><%= current_user.email %></span>
      <% else %>
        <br><span><%= link_to 'Войти', new_user_session_url, :class => "btn btn-success btn-sm" %> <%= link_to 'Регистрация', new_user_registration_url, :class => "btn btn-warning btn-sm" %></span>
      <% end %>
    </div>

    <p id="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
